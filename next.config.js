/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports ={ 
  images: {
  domains: ['www.themealdb.com', "edamam-product-images.s3.amazonaws.com"],
},
}

